// Package agentread is the read-only MCP surface over one account's focus history.
// The account is the session token resolved by the caller. Tools do not accept an
// email or user id, and the queries behind Reader are scoped to that account.
package agentread
