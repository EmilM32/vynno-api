#!/usr/bin/env bash
set -euo pipefail
# shellcheck source=lib.sh
. "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/lib.sh"

stop_api
# Stop the container; keep the named volume.
"${COMPOSE[@]}" stop
echo "stopped"
