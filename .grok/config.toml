# Local read-only MCP. The session token lives in .env (VYNno_MCP_TOKEN), which
# cmd/mcp loads. Do not put the token in this file. See docs/adr/0016-readonly-mcp.md.

[mcp_servers.vynno]
command = "go"
args = ["run", "./cmd/mcp"]
cwd = "."
startup_timeout_sec = 180
